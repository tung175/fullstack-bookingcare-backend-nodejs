<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Display CRUD</title>
</head>
<body>
    <table class="w3-table-all w3-hoverable">
        <thead>
            <tr class="w3-light-grey">
                <th>Email</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>address</th>
                <th>action</th>
            </tr>
        </thead>
        <tbody>
            <% for(let i=0; i < dataTable.length; i++) { %>
                <tr>
                    <td>
                        <%= dataTable[i].email %>
                    </td>
                    <td>
                        <%= dataTable[i].firstName %>
                    </td>
                    <td>
                        <%= dataTable[i].lastName %>
                    </td>
                    <td>
                        <%= dataTable[i].address %>
                    </td>
                    <td>
                        <button type="button"><a href="/edit-crud?id=<%= dataTable[i].id %>"
                                target="">Edit</a></button>
                        <button type="button"><a href="/delete-crud?id=<%= dataTable[i].id %>"
                                target="">delete</a></button> 
                        <!-- <form action="/delete-user" method="POST">
                            <input type="text" name="userId" hidden value="">
                            <button type="submit">Delete</button>
                        </form> -->
                        <!-- <form action="/edit-crud?id=<%= dataTable[i].id %>" method="GET">
                            <button type="submit">Edit</button>
                        </form> -->

                    </td>
                </tr>
                <% } %>
                    </tr>
        </tbody>
    </table>
</body>
</html>